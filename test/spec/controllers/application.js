/*
describe('ApplicationCtrl', function () {
  
  var scope, ctrl;

  beforeEach(module('paradropApp'));

  beforeEach(inject(function($controller) {
    scope = {};
    ctrl = $controller('ApplicationCtrl', { $scope: scope });
  }));

  it('should have myTEST_TEST set to 600', function () {
    expect(scope.myTEST_TEST).toBe(200 * 3);
  });

  it('should not have a current user on startup', function () {
    expect(scope.currentUser).toBe(null);
  });
});
*/
